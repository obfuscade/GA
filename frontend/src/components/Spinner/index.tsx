import { JSX } from "react";
import * as Styled from "./styles";

function Spinner(): JSX.Element {
  return (
    <Styled.Wrapper>
      <Styled.Spinner />
    </Styled.Wrapper>
  );
}

export default Spinner;
